{
  "name": "autowater-build",
  "version": "1.0.0",
  "description": "Build tools for Autowater ESP32 project",
  "private": true,
  "scripts": {
    "minify": "node minify_web.js",
    "build": "npm run minify",
    "watch": "nodemon --watch  -e html,css,js --exec npm run minify"
  },
  "devDependencies": {
    "html-minifier-terser": "^7.2.0",
    "terser": "^5.26.0",
    "clean-css": "^5.3.3",
    "nodemon": "^3.0.2"
  }
}
